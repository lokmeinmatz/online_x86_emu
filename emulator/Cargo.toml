[package]
name = "emulator"
version = "0.1.0"
authors = ["matthiak00 <matthias.kind@fu-berlin.de>"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
